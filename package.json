{
  "name": "kgarsjo-com",
  "private": true,
  "scripts": {
    "deploy": "lerna run deploy",
    "release": "npm install && lerna bootstrap && lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.13.4"
  }
}
